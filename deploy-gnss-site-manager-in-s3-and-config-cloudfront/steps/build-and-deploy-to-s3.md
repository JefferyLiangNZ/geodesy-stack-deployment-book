Install nix tool, set up the nix environment.

Clone the source code

```
git clone git://
cd GNSS-site-manager
git checkout master

# install all dependencies
npm install -S -D

# configure 
# build the target bundle for deployment
```



